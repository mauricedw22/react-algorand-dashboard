import React from 'react';

const AlgoBlockData = (props:any) => {

    return (
        <React.Fragment>
            <div>
                <h1>{props.round}</h1>
            </div>
        </React.Fragment>        
    );
};

export default AlgoBlockData